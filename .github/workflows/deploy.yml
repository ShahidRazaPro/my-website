name: HTML CI Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Print files (test step)
      run: ls -la

    - name: Success message
      run: echo "Website CI pipeline running successfully 🚀"
